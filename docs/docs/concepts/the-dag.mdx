---
title: The DAG
description: Nodes as vertical slices, edges as real GitHub blocking relationships, and the frontier that decides what runs next.
sidebar:
  icon: git-branch
  order: 2
---

The plan as a graph of **nodes** joined by **edges**. Laid down by `/dag:chart` from a grilled plan; the
suite then validates it, walks it, and closes it.

## Atlas and expedition

When the destination itself is unclear, `/dag:plan` Wayfinds before it charts. The durable artifact is
one `dag:atlas` issue: decisions, unspecified territory, and links to related expedition charts. An
**expedition** is the bounded journey one chart covers—one destination, one map, one DAG.

The Atlas stays above its charts; it never duplicates their node detail. Local uncertainty about the
route to a known destination remains on the chart as fog-of-war. Uncertainty about the destination
returns to the Atlas.

## Node

One work slice: a narrow-but-complete vertical path through every layer — schema → logic → UI → tests —
sized for a single fresh context window and demoable on its own.

**One node = one teammate = one worktree = one PR.**

<CardGroup cols={2}>
  <Card title="Vertical, never horizontal">
    A node cuts a narrow path through every layer. A slice of one layer is not a node.
  </Card>
  <Card title="Demoable alone">
    If you can't show it working by itself, it isn't a tracer bullet.
  </Card>
  <Card title="One context window">
    Sized so a single fresh session can carry it to a merge-ready PR.
  </Card>
  <Card title="Prefactoring comes first">
    "Make the change easy, then make the easy change" — the easy-making is its own node.
  </Card>
</CardGroup>

### Wide refactors are the exception

A mechanical change whose blast radius fans across the codebase — rename a shared symbol, retype a
column — can't land green as one vertical slice. Sequence it **expand → migrate in batches →
contract**: expand adds the new form beside the old so nothing breaks; each batch is its own node
blocked by expand, green throughout because the old form survives; contract deletes the old form,
blocked by every batch.

## Edge

The tracker's native blocking relationship, not prose. This is GitHub's real issue-dependency relation —
a REST endpoint, not a `gh issue` flag, and it takes the issue's **database id**, never its `#number`:

```bash
gh api repos/<owner>/<repo>/issues/<n> --jq .id                       # the database id
gh api -X POST repos/<owner>/<repo>/issues/<blocked>/dependencies/blocked_by \
  -F issue_id=<blocker-database-id>
gh api repos/<owner>/<repo>/issues/<n>/dependencies/blocked_by        # read the blockers back
```

The readback carries each blocker's `state`, so the frontier costs one query per node.

:::warning
Prose edges — a "Blocked by #12" line in the body — are the failure this suite exists to prevent, not a
degraded mode of it. The edges are the product.
:::

## Map

One issue labelled `dag:map`: an **index**, not a store. It lists each node once and links to it; detail
lives in the node and is never restated on the map. Nodes are the map's sub-issues:

```bash
gh api repos/<owner>/<repo>/issues/<map-number>/sub_issues
```

The map also carries the two profiles that make a run reproducible across context windows — the
[**proof profile**](/concepts/proof#the-proof-profile) and the **run profile** (concurrency cap, model
per role, autonomy level).

## Frontier

The open, unblocked, unclaimed nodes — every node whose blockers are all closed. It is the executable
edge of the graph, and it is what sizes a wave: three focused teammates beat five scattered ones, and a
wave of four ready nodes wants about four.

The main orchestrator computes that frontier from GitHub and assigns it. A host task list is only the
orchestrator's record of current assignments; it is not another graph, and workers never self-claim the
next node from it. When a rung-3 stop exposes a broken premise, the orchestrator freezes new dispatch,
marks the map halted and checks in-flight nodes before allowing only work proven independent of the nest.

## Readiness

How knowable a node is, decided while charting. A node that isn't **clear** gets a second issue — the
**de-fog node** — that blocks it until the unknown is settled:

| Readiness | The de-fog node | Its label |
| --- | --- | --- |
| **clear** | none — build it | *(none)* |
| **needs-grilling** | a decision to settle, via `/dag:grill` | `dag:needs-grilling` |
| **needs-research** | a fact to find, via `/dag:research` | `dag:needs-research` |
| **needs-prototype** | a **spike** to answer it, via `/dag:prototype` | `dag:needs-prototype` |

:::warning
The label goes on the **de-fog node**, never on the build node it blocks. `/dag:plan` only ever looks at
the frontier, and a build node blocked by its de-fog node is by definition not on the frontier — so a
label there is a label nothing will ever read.
:::

The de-fog node is a real child issue with a real blocking edge, which is what keeps the frontier from
ever surfacing a build node whose premise is still unsettled.

**Closing the de-fog node is the whole mechanism.** When its move lands, `/dag:plan` records the answer
as a comment on it and closes it. That one close unblocks the build node onto the frontier *and* retires
the label — no one ever removes a label, because a closed node is never on the frontier. A de-fog node
left open leaves its build node blocked forever.

## Spike

A throwaway prototype — code that answers one design question, then is discarded — raised as its own
node that blocks a `needs-prototype` build node until it resolves. It de-risks the unknowable cheaply,
before the real build commits.

The validated decision is folded into the real code; the spike itself is captured on a throwaway branch
as a primary source, referenced from the node's issue.
